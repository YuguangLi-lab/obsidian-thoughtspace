# 更新记录

## 0.98.6

- 重新整理侧栏快捷入口、搜索与白板列表的视觉层级，统一对齐、选中状态和窄栏间距。
- 顶部格式栏采用紧凑字段分区，恢复被旧响应式样式隐藏的标签，继续保持单行悬浮与横向滚动。
- 修复创作工具栏分隔线不显示；选择、创作、组织和更多工具的分区更清楚。
- 底部画布控制在窄窗压缩文字，极窄窗采用明确的两行布局，避免与缩放控件挤占。

## 0.98.5

- 空白处按住左键拖动即可框选；Shift 累加选择，空格加左键或中键平移。取消框选恢复之前的选择。
- 多选后可在顶部切换“对象 / 连线”，统一修改对象样式，或批量修改连线路径、箭头、虚实线与颜色。
- 默认仅修改所选对象之间的连线，可显式切换“所有相连”；跳过锁定、隐藏或已删除的端点，防止旧工具栏误改重接后的连线。
- 批量目标连线高亮，每次修改作为一次撤销操作；框选期间不反复重建格式工具栏，沿用按帧合并的指针更新。

## 0.98.4

- 整理左侧快捷入口：空间总览、笔记摘录、整理白板和分组预览合并为两列工具区，减少重复边框，统一按钮与文字对齐。
- 优化搜索框、白板树行高和窄侧栏间距，保持原有导航、收藏、整理和分组入口。
- 减淡网格与点阵，画布底色跟随 Obsidian 主题；吸附状态保留颜色区分，并兼顾高对比度偏好。
- 顶部格式选择增加清晰的控件边界，统一悬浮工具栏圆角；保留单行滚动及原有编辑、媒体尺寸和连线逻辑。

## 0.98.3

- 修复 Obsidian 原生搜索结果打开索引 Markdown 而不是白板的问题；直接在搜索结果标签页打开实际白板。
- 点击白板内容命中项时定位对应对象；标题命中打开整张白板。保留普通 Markdown 笔记的原生打开行为。
- 对手动修改的索引、未保存编辑、已删除白板、快速切换和插件卸载增加保护，避免误跳转或覆盖编辑。

## 0.98.2

- 修复图片与 PDF 内容比外层卡片小、选中框和连线端点远离实际内容的问题。按媒体原始比例更新实际节点尺寸，不再仅隐藏边框。
- 删除图片插入时额外的 42 像素标题占位；PDF 按当前页尺寸适配，切换横竖页保留选定宽度。
- 图片、PDF 拖动缩放默认保持比例；异步测量沿用过期检查、锁定与折叠保护，避免覆盖用户正在进行的拖拽。

## 0.98.1

- 图片卡片移除文件名栏、底色和装饰边框，完整展示图片，不裁切内容。
- PDF 展开时只显示页面，翻页和折叠按钮在悬停或键盘聚焦时浮现；保留折叠状态下的文件标识及快捷键。
- 侧边“更多白板工具”增加“连线统一为直线”；选中连线后也可将当前路径应用到全部连线。
- 当前白板后续新连线、导图分支及导入连线沿用统一路径；支持撤销，并保留颜色、箭头、说明和关系结构。

## 0.98.0

- 增加 Obsidian 原生搜索索引：白板文字、标题、分组和连线说明支持搜索，并通过深链接定位节点。增量同步、内容校验与覆盖保护保留手动修改。
- PDF 卡片支持折叠、恢复尺寸和页码；选中卡片后可使用 PageUp / PageDown / Home / End 翻页。
- PDF 文档短期复用，限制闲置缓存和画布尺寸；取消渲染、文件更新、关闭视图和插件卸载时清理资源。
- 父子分支支持逐层展开和全部展开；普通连线可显式转换为父子分支，校验循环与多父节点。
- 新增搜索同步、PDF 生命周期、折叠和 5,000 层分支边界测试。

## 0.97.2

- 修复社区目录审核指出的静态样式设置与设置页标题问题，保留现有卡片、导图和写作布局。
- 将最低 Obsidian 版本改为已完成原生验收的 1.13.7，避免承诺未经验证的旧版兼容性。
- 修复文件列表定位时未等待延迟视图加载的问题。
- 补充英文功能与安装说明，并在 CI 中加入官方 Obsidian ESLint 检查。

## 0.97.1

- 将插件显示名称改为 `ThoughtSpace Whiteboard`，符合社区目录仅使用 Basic Latin 字符的命名规则。
- 插件 ID 保持 `thoughtspace`，沿用现有安装目录及设置。
- 此版本仅更新发布元数据和说明，白板运行代码与 0.97.0 一致。

## 0.97.0

### 连线

- 修复鼠标位于卡片内部却吸附到相邻卡片的问题；空隙中选择最近目标。
- 吸附范围按屏幕 20 px 计算，修正缩放与边角范围。
- 复用可见节点和连接期间的索引，减少重复画布测量与路径计算。
- 保留按帧合并输入、松手最终落点、重接、取消及撤销重做。

### 思维导图

- 拖线到空白处直接创建子主题、连接、排版并进入编辑。
- 增加方向键切换主题、Shift+Tab 返回父主题、F2 原位编辑。
- Tab / Enter 连续输入支持中文输入法组合输入保护。
- 新主题先测量再统一布局，避免重复整理。
- 优化草稿历史大小统计，保留 20 条 / 16 MB 上限。
- 修复相同文字的确认操作错误清除重做历史。
- 普通孤立对象保持方向键微调；连线等待状态不触发主题导航。

### 验证

1142 项自动测试通过，TypeScript 检查及构建通过。Obsidian 原生流程检查 34 项、真实鼠标/键盘检查 6 项通过。独立日历插件未包含在此发布包中。

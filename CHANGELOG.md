# 更新记录

## 1.0.16

- 白板工具栏仅更新已改变的控件属性与文字；未选择对象或连线时跳过无关全板扫描。状态仍从当前 DOM、选区和设置读取，保留重挂载与外部修改后的同步。
- 离屏连线按原有几何边界逐项短路检查，减少不必要的尺寸读取和边界对象分配；跨越视野的线、端口及选中状态保持原行为。
- 思维导图自动主题的备用尺寸计算直接按 Unicode 代码点累加，省去逐行字符数组；导出 SVG 时连线路径和标签共享同次路由结果。
- 笔记选中文字建立关联时，Markdown 行解析只消费至选中行；仍使用完整原文，保留属性前瞻、代码、链接和隐藏注释的校验。
- 笔记片段多词搜索同次复用归一化内容，空查询跳过全列表过滤；保留当前定位、无结果状态和更新后即时读取。

## 1.0.15

- 框选一次收集命中对象；无需处理折叠遮挡时跳过全板投影与父子关系计算。保持分组完整包含、折叠显示范围和选择顺序。
- 对齐辅助线精确命中后停止无意义的候选搜索，保留原来的轴顺序与等距优先级。
- 整理布局按标题排序时，同次操作内每个对象只提取一次标题；整理当前视野先排除离屏对象，再检查分组包含关系。布局应用仍校验锁定及过期预览。
- 资料库筛选与标签统计共享同次同步读取，减少元数据查询；异步载入后的卡片标签仍读取当前 Obsidian 元数据。
- Markdown 行不含注释起始标记、也不在跨行注释内时，跳过字符数组和注释遮罩解析；代码、注释与列表边界规则保持一致。
- 普通正文搜索省去整词边界的 Unicode 扫描；开启整词匹配时继续检查完整边界。

## 1.0.14

- 连线两端已指定端口时直接使用现有端口；折线圆角与标签位置共享本次路径的段长，减少拖动时重复计算，保留全部路由和端点行为。
- 大纲批量导入在首次合法主题时读取一次根主题的自动布局设置；按关系整理布局在同次预览内共享规范化连线对，应用时仍重新校验过期预览。
- 白板缩略图一次收集范围、端点索引和稳定绘制顺序；长文本标题只读取显示所需的开头，避免为短标题切分整篇正文。
- 修复对象类型改变但位置与尺寸不变时，缩略图仍沿用旧分组样式、层级或透明状态的问题。
- 普通正文不含来源标记时跳过逐行来源解析，保留原有换行归一；含来源标记的笔记、PDF 摘录和代码示例沿用完整识别规则。

## 1.0.13

- 白板导航只从已打开会话提取子白板链接，不再为刷新侧栏复制整板正文、布局和写作草稿；普通可编辑副本读取仍保持隔离，取消搜索不发布不完整关系图。
- 连线渲染逐项比较已有几何与样式状态，省去每帧重复的字符串键和临时索引数组；位置、颜色、方向、选中与缩放仍读取当前值。完整刷新中的对象统计改为一遍计数。
- 批量添加同级主题一次合并连线顺序，保留原有主题顺序、校验和自动布局；Canvas 导出按需复用端点查找，避免长连线列表反复扫描节点。
- 编辑尺寸测量在新草稿真正开始测量时及时取消旧任务，避免旧 Markdown 渲染或公式排版阻塞当前输入；同一稿件继续共享任务，保留输入防抖和关闭清理。
- 普通静态段落的等效预览复用最近一次成功测量；外观、字体、尺寸、挂载位置变化重新测量，动态嵌入、图片与富 Markdown 沿用原生渲染路径。

## 1.0.12

- 拖动中仅在连线或网格落点真正需要时构建白板几何投影，空连线场景仍清理过期连线与端点；节点重排后修正已验证的目标位置，后续帧继续读取实时对象。
- 连线移动跳过未变化的选中与聚焦 CSS 状态调用；样式、主题、批量选择及聚焦转换仍及时更新。
- 折叠/展开子节点和批量选区展开共用轻量父子关系工厂，省去无关的全板可见性与分组几何计算；保持逐层展开、锁定、循环守卫及移动语义。
- 白板搜索将正文与各实例标题分开索引，同一长文只归一化一次，单次查询共享正文词匹配；保留每个引用的标题、位置、筛选、摘要及跳转。元数据仅在同次重建内复用，刷新立即读取新内容。
- 修复快速切换或关闭白板后，迟到加载结果重新挂载旧会话、覆盖新页面或遗留监听的问题；旧加载与卸载回调只清理自己的会话。
- 修复保存过程中收到外部修改通知却未复核的问题：等待保存队列结束后合并读取，保持写入前冲突保护和恢复草稿机制；自身写入通知不触发额外写回。

## 1.0.11

- 白板平移和缩放时跳过空编辑目标的全板查找，仅检查已挂载卡片的标题编辑器；在同一次刷新中复用检查结果，保留离屏草稿、绘制顺序和实时折叠状态。
- 按需计算连线父子拓扑：可见卡片复用且无需折叠控制更新时，不再重复构建关系索引；新节点入窗、详情级别和折叠变化仍实时更新。
- 整理布局的预览和应用在各自操作内复用折叠移动单元，减少重复扫描；保持预览过期、锁定、隐藏子节点和分组包含关系的写入前校验。
- PDF 卡片预览支持各阶段即时取消和 15 秒总等待预算，超时可重试或打开原生阅读器；后台工作线程无响应时不再占住后续缩略图队列，失败画布及时释放，共享文档仍按租约复用。
- 修复 PDF 工作线程同步抛错时中断其他文档清理，以及普通笔记的搜索/关联弹窗关闭异常时遗留工具栏监听与界面的问题。
- Markdown 工具栏仅在状态实际变化时更新关联笔记按钮属性，减少连续调整选区期间的重复 DOM 写入。

## 1.0.10

- 普通笔记的 Markdown 工具栏按原生编辑器的不可变文档身份复用只读正文；移动光标和改变选区不再重复读取整篇长文，编辑、撤销、切换和卸载及时失效，格式化与冲突检查仍读取实时内容。
- 框选先做几何预检，空白和零面积选择跳过无关的折叠关系计算；保持分组全包含、折叠显示尺寸、隐藏节点、Shift 累加与筛选行为。
- 重接连线复用经验证的边位置，仍逐帧读取当前边与样式；长文本目标提示先截短再解析，减少持续拖动时的扫描与字符串开销。
- 分组移动预览在单次操作内共享节点和空间索引，保留原来的折叠分支完整性检查、首个冲突提前拒绝和锁定规则；后续操作重新建立索引。
- 文本框的 Markdown 预览改用已挂载的原生渲染容器，并保留实际根节点，修复原生后处理器无法及时完成或根部滚动、方向与事件丢失的问题；失败恢复原文，取消隔离迟到结果，替换时保留引用徽标及其事件。

## 1.0.9

- 思维导图自动重排检测共用事务内的父子索引，减少无关的分组折叠、空间包含和兄弟顺序扫描；保持手动位置、锁定、混合分组及撤销行为。
- 网格拖动时，落点提示与连线共用本帧的显示数据，减少大量节点的重复投影；落点不变时跳过重复文字、可见性及相同样式写入，保留小数缩放精度。
- 保存队列合并重复的“保存中”状态通知，减少多个视图的无效刷新；仍逐次保留内容更新、最终保存确认和冲突恢复。
- 卡片编辑的自动尺寸测量增加一秒总预算、关闭取消、原生公式排版等待及独立窗口计时器；异常初始化和过期结果均清理，避免新草稿长期排队或被旧尺寸覆盖。
- 文本与卡片的每次尺寸测量独立释放取消信号，避免在长时间打开的编辑器内累积异步回调。

## 1.0.8

- 修复后台标签页中的卡片被测为 2 像素宽、触发尺寸校验异常的问题；隐藏时暂缓测量，重新显示后按当前内容补做自适应，并在写入前过滤低于白板最小尺寸的结果。
- 白板刷新在同一帧内共用父子关系与折叠可见性计算，减少画面、连线、内容定位和缩略图的重复扫描；编辑草稿和拖动布局分别计算，避免旧几何影响连线。
- 笔记卡片预览补充取消与超时处理：移出画面、切换白板或渲染器长时间无响应时释放预览队列，避免后续卡片一直等待。
- 每次笔记预览使用独立的渲染容器；超时或销毁后的迟到结果不会覆盖重试内容，保留 Mermaid 图表、表格滚动、来源、只读任务、自适应尺寸及原位编辑。

## 1.0.7

- 重构选中对象工具栏：Markdown、文字、背景、边框分类就近切换，按卡片、文本、分组及媒体对象显示适用控件，保留单行悬浮与窄窗滚动。
- Markdown 工具栏调整常用顺序，下划线、删除线、代码和缩进等收进更多格式，保留全部格式能力；普通笔记和白板使用同一套布局。
- 白板背景合并为显示当前状态的单一入口，集中切换点阵、网格、纯色、纸张与图片，并直达纸张和背景图片设置。
- 分类切换保留原位编辑与选区，外观偏好只保留在当前视图；加强过期选区、锁定、输入法组合与保存中的回调保护。

## 1.0.6

- 分组框增加背景色、透明样式与任意自定义背景色，选中一个或多个分组即可在顶部外观工具栏修改。
- 增加分组标题分割线，可选实线、虚线或点线，沿用分组边框颜色与粗细；折叠后保留背景，展开恢复分割线。
- 分组外观支持保存、撤销重做与样式复制，批量修改分组不影响内部笔记；旧白板保留原有默认外观。
- 加强工具栏对过期选择、锁定对象和无效选项的保护，修复分组默认边框线型的显示与折叠后刷新。

## 1.0.5

- 单条与批量连线共用紧凑外观工具栏，统一路径、箭头、线型和颜色控件；关系说明、跳转端点与全板路径操作分区排列，适配窄窗口滚动。
- “更多白板工具”新增本次打开白板期间最近使用的 3 项工具，继续支持搜索与键盘选择，隐藏或禁用的操作不会被快捷入口绕过。
- 修复旧连线控件在切换选择、删除连线或锁定端点后仍可能修改内容的问题；锁定、解锁和折叠状态变化及时更新工具栏。
- 优化无折叠白板的批量连线筛选，避免重复构建分支可见性索引；保留折叠、锁定及撤销保护。

## 1.0.4

- 顶部外观工具栏整理为卡片、文字和边框三组，紧凑模式用图标与当前值减少重复标签，保留单行操作、原生选择器及舒适模式。
- 侧栏统一搜索、导航与树形列表的间距，加大展开按钮的点击范围，改善窄栏中的标题空间和选中层级。
- 底部缩放区新增“聚焦所选”快捷按钮，有选择时出现；保留原有视角历史和编辑草稿。
- 状态摘要仅在变化时更新，减少重复 DOM 写入和读屏播报；修复从右到左布局中工具栏箭头滚动方向与端点禁用状态错误。

## 1.0.3

- 分组可作为文本、笔记、图片、PDF 及子白板的父级或子级；折叠、逐层展开、全部展开保留分组内容及独立折叠状态。
- 分组标题直接显示“内容”和“子节点”操作；子节点按钮常驻并显示数量，就近菜单可逐层或全部展开、转换后续连线，减少右键查找和悬停丢失。
- 校验包含关系与父子连线组合的循环、单父级和锁定限制；折叠后的普通父节点移动时携带子分组及材料，保护未保存编辑，支持撤销。
- 含分组的混合树保持现有位置；自动主题排版不再移动分组而遗留内部内容，手动主题排版提示改用分组布局。
- 新建表格默认透明；表头、行和单元格均沿用白板或自选卡片底色，保留无边框样式和原生编辑的选择反馈。

## 1.0.2

- 整理侧栏、创作工具栏、悬浮编辑栏与底部控件的间距和层级，统一紧凑尺寸与轻量边框，继续跟随 Obsidian 字体和主题。
- “更多白板工具”支持即时搜索、方向键选择、Enter 执行与 Esc 清空/关闭；搜索栏固定，工具列表独立滚动，减少短窗口中的遮挡。
- Markdown 编辑栏合并重复标题入口，保留全部六级标题；外观切换固定在工具序列开头，按 Esc 可回到原位编辑并保留草稿。
- 白板与普通笔记的编辑工具栏支持滚轮浏览超出宽度的工具；方向键只滚动工具栏，不再带动外层笔记与白板视图，保留原生选择框及缩放手势。
- 修复嵌套 Markdown 工具组的键盘导航范围及中文输入法与工具搜索的快捷键冲突。

## 1.0.1

- 文本框改用 Obsidian 原生 Markdown 渲染与原位实时预览，支持标题、列表、引用、提示块、代码、链接、行内公式和独立公式；保留原生正文字体与文本样式。
- 新增侧边“表格”入口和空白处右键“在此添加表格”；生成三列表格并选中首个表头，保持当前视角。文本与笔记的编辑工具栏支持表格和独立公式插入。
- 白板内的表格使用无边框样式，文本、笔记卡片和原位编辑统一通过留白与轻微表头底色区分内容。
- 自适应尺寸改为测量实际渲染的 Markdown，包括公式、表格和来源符号；编辑时合并测量请求，旧草稿与关闭后的异步结果不会覆盖当前尺寸。
- 编辑中为 Markdown 语法和原生表格临时扩展显示高度，保存后恢复渲染所需尺寸；失效或超时的预览及时释放渲染队列。
- 窄文本框中的表格、代码块和公式可以独立滚动，避免滚动内容时误移动白板；带 Ctrl / Cmd 的缩放操作继续交给白板。
- 思维导图编辑表格、列表、公式或代码时保留 Markdown 的 Enter / Tab 行为，单行普通主题仍可连续创建同级和子主题。
- 文本框中的相对链接使用所在白板路径解析；阅读状态的任务复选框只读，双击进入编辑后修改，避免误写白板源文件。

## 1.0.0

- 新增可自定义纸张背景：米黄、纯白、象牙白、牛皮纸、再生纸和自选纸色，提供实时预览与 0–100% 纹理强度；静态细颗粒与短纤维保持屏幕尺寸，不影响平移、缩放和网格吸附。
- 新增本地背景图片设置：PNG、JPEG、WebP、GIF，可替换、移除，选择填满、完整显示或平铺，调节透明度；应用前只预览，图片复制到本库插件目录并按内容复用。

- 分类整理支持生成真实命名分组框，可按对象类型、颜色、阅读状态或连通关系组织材料；预览包含折叠后代边界，应用后可一次撤销。
- 新增个人布局预设：保存、选择、覆盖、重命名和删除常用排版参数，跨白板复用，默认保持当前视角。
- 新增“移入已有分组”：批量移动卡片、文本、图片及分组，预览目标扩框；保持相对位置和连线，拒绝锁定、循环包含、过期预览与吞入无关对象的移动。
- 侧栏大纲升级为真实分组层级，支持收起/展开导航、显示后代数量与搜索时保留父级路径；导航收起不改变白板内容的折叠状态。
- 新增常用视角管理器：搜索、位置示意、重命名、更新位置、排序、删除与快速定位，管理操作纳入白板撤销。
- 重整顶部工作区入口、侧栏层次与底部操作区；编辑栏保留原位，次要工作区操作集中到菜单，窄分屏减少挤压。
- 新增固定于屏幕的白板起始页，提供文本、已有笔记和导图模板入口，平移缩放时不重建或缩放欢迎内容。
- 整理前安全保存正在编辑的内容，关闭或切换白板时关闭所属弹窗并取消过时的打开请求。
- 保留原生正文字体、透明卡片、独立日历样式隔离、新建对象原位编辑和已有输入自定义。

## 0.98.31

- 在折叠分组覆盖的范围内新建文本或卡片时展开所属祖先，避免退出编辑后新对象被隐藏；同时保持白板平移和缩放不变。

## 0.98.30

- 新建文本、卡片及思维导图主题后直接原位编辑，保持当前白板平移与缩放，不再自动以新对象为中心跳转或放大。
- 保留即将编辑的节点，避免视口边缘新建主题被虚拟化提前移除；显式定位对象和编辑已有卡片继续使用原有导航行为。

## 0.98.29

- 双击白板空白处直接创建文本框并进入编辑，位置跟随鼠标并正确换算平移和缩放。
- 仅响应两次没有拖动的空白点击；拖动、框选、连接端口或操作已有对象后不会误建文本。已有草稿先安全保存，冲突或切换白板时不创建重复对象。

## 0.98.28

- 文本框支持折叠与展开，保留完整原文及原有尺寸；公式使用 Obsidian 原生 LaTeX 渲染，支持行内 `$...$` 和独立 `$$...$$`，完成渲染后再自适应，限制并发并取消过时的渲染。
- 分组支持连接后设为父子分支，分别控制组内内容与后代分组的折叠；收起后拖动会带上后代及其内容，继续遵守锁定、单一父组与循环关系校验。
- 思维导图增加向上布局和向上组织图预设，导入大纲、添加子主题、折叠展开及自动排版保持相应方向。
- 修复折叠文本套用自动模板时尺寸校验失败，以及导入树继续添加节点后方向被白板默认布局覆盖的问题。
- 修复插件重载时样式尚未加载便提前测量卡片的问题：样式实际就绪后再自动适应，避免笔记卡片尺寸突然缩小。

## 0.98.27

- 白板搜索、切换侧栏或关闭页面后及时停止过时的白板关系扫描，避免继续读取无用结果；嵌套关系校验仍完整执行。
- 修复自适应测量恢复原尺寸后仍应用旧待处理尺寸的问题，避免字体加载或连续测量后卡片突然被撑大。
- 修复查找或关联笔记弹窗关闭异常时阻断编辑器清理、并把成功保存误报为失败的问题；其他资源仍逐项释放。

## 0.98.26

- 可见卡片的预览优先级和分组层级改用稳定分桶，减少平移、缩放时的重复排序；选中优先、预览数量及对象叠放顺序保持一致。
- 编辑文本自适应时仅写入发生变化的位置和尺寸，减少重复样式写入，同时保留原生编辑器尺寸同步。
- 没有连线时跳过连线图层的节点索引构建，并及时移除旧连线；重新添加连线时正常恢复。

## 0.98.25

- 保存笔记及更新其元数据时，按侧栏当前页面刷新：资料库和任务继续同步，白板树与大纲跳过无关正文更新；新建、删除、重命名及白板结构变化仍及时刷新。
- 同一笔记被多张卡片引用时，在一次白板渲染中共用标签与属性读取结果；后续刷新重新读取当前文件状态，保留删除、重新关联和属性更新的同步。
- 编辑器切换多光标选区时不再递归发送重复选区通知；保存、组字和正文变化仍更新状态及格式工具栏。

## 0.98.24

- 平移和缩放时，已挂载且预览级别不变的卡片跳过重复的子节点候选扫描；新内容进入视野、切换详细预览及完整刷新时仍重新计算。
- 未单独选中连线时跳过端点查找，保留批量连线高亮与取消选择后的端点清理，减少大白板每帧重复遍历。
- 缩略图仅更新真正变化的视口属性，并移除无用的选区排序；侧栏宽度、缩放、视角恢复及点击定位继续使用当前几何。

## 0.98.23

- 固定尺寸的 Markdown 笔记卡片调整大小时保留现有正文预览和组件，让正文直接随宽度重排，减少重复读取与预览闪动。
- 连续调整大小、撤销重做、折叠展开仍使用最新尺寸；自动适应、锁定、正文及来源变化继续校验并更新预览，防止旧异步结果覆盖新状态。

## 0.98.22

- 笔记、文本、图片和 PDF 卡片修改颜色、透明度及边框线型时直接更新外观，保留已有预览与组件，减少重复加载和预览闪动。
- 已有卡片及标题编辑中的卡片即时同步边框颜色和文字颜色，撤销、重做后也使用当前颜色。
- 尺寸、线宽、字体、正文、文件、PDF 页码、锁定和折叠变化继续重新校验预览；纯外观修改保留仍有效的自适应测量，失效测量继续拒绝应用。

## 0.98.21

- 框选时仅更新选中状态真正变化的卡片、连线和大纲行，减少连续扩大选区时的重复样式更新；保留批量连线范围与锁定校验。
- 大白板移到内容之外时，“回到内容”按钮每次刷新只读取一次画布宽高，避免按卡片数量重复读取布局。
- 原生 Markdown 草稿按不可变文档复用正文读取；选区移动和多个界面组件读取同一份正文时避免重复展开全文。输入、撤销重做、编辑器替换及关闭均重新校验或释放缓存。

## 0.98.20

- 白板卡片定位采用差量样式更新：拖动和视口刷新只写入真正变化的位置、大小与外观，保留透明卡片、字体、边框和编辑中尺寸。
- 连线标签在文字不变时复用已测量宽度与 SVG 元素；拖动端点只更新几何，标签修改、撤销及离屏重建仍使用最新内容。
- 批量格式工具栏在同一次渲染中复用连线扫描结果，减少大选区重复扫描；应用操作时仍重新校验锁定、重连和选区。
- 连续 Markdown 格式操作在编辑器已经聚焦时减少一次全文和选区读取；切换窗格或实际聚焦后继续校验内容，避免使用过期选区。

## 0.98.19

- 白板拖动、调整大小、框选和连线在松手时只计算最终位置，减少重复预览；保留快速拖出再返回时的手势判断，避免误触菜单或添加子主题。
- 其他触点松开或取消时，不再提前消费当前拖动的更新帧，保持多指与鼠标事件互不干扰。
- 白板搜索刷新及正文索引完成后，保留“复制链接 / 右侧打开”等行内按钮焦点；对象移除时回到搜索框。
- 搜索分页复用已生成的短摘要；关键词、筛选或正文索引变化时重新计算，减少长笔记的重复处理。
- 笔记查找替换复用经过内容校验的匹配结果，逐次替换合并为一次搜索与预览刷新；保留原生撤销、中文输入法和编辑冲突保护。
- 修复替换触发窗口关闭后仍保留全文快照、重新聚焦已关闭窗口的问题。

## 0.98.18

- 重整白板内容搜索面板：突出标题、分组和正文摘要，固定搜索与底部操作区域，结果单独滚动，兼容窄窗口及明暗主题。
- 搜索支持一键清除关键词与筛选，以及 Cmd / Ctrl + Enter 在右侧打开笔记；资料库列表增加直接在右侧打开笔记的按钮。
- 卡片库和大纲增加清除筛选入口，空结果可一键恢复；清除后保留排序偏好并回到搜索框。
- 搜索结果方向键导航复用当前结果，避免每次重建整个列表；完善输入法、分页与异步打开的交互保护。
- 修复资料库刷新时预览按钮失去键盘焦点的问题，按笔记路径恢复原行焦点与滚动位置。

## 0.98.17

- 统一侧栏卡片列表、筛选栏与空状态的间距和层级，减轻重复边框与色块，兼容窄侧栏及明暗主题。
- 白板操作面板加入“仅可用”筛选、结果数量与清除筛选入口，改善分类、搜索、键盘焦点和禁用状态的呈现。
- 侧栏搜索可用方向键进入结果，结果列表支持上下移动与 Esc 返回搜索；保留原有打开、定位和添加引用操作。
- 修复中文输入法确认候选词时误执行白板操作的问题，保留搜索框 Home / End 与系统快捷键；防止命令重复执行。
- 每次侧栏刷新只建立一次笔记引用索引；操作搜索复用索引，方向键只更新前后两行，减少重复扫描与样式写入。

## 0.98.16

- 文本卡片支持顶部工具栏的预设底色、自定义颜色与透明样式，可与笔记卡片批量修改或复制外观。
- 文本颜色保存、重载、撤销和缩略图同步，保持既有文字、边框、尺寸与编辑逻辑。

## 0.98.15

- 新增“鼠标与键盘”设置页：左键、右键和中键可分别选择平移、框选或不操作，提供默认、左键框选、触控板三种方案，以及滚轮方向、平移速度、缩放中心、灵敏度和拖动防误触距离。支持独立恢复默认。全部说明使用简体中文。
- 新增 22 个白板命令，可在 Obsidian 原生快捷键设置中绑定，涵盖新建、撤销重做、分组、副本、移除、思维导图主题、搜索、阅读及视角操作。增加方向键微调步长和删除键开关；编辑文字时不抢占按键，保留思维导图结构操作。
- 分组框支持折叠/展开、右键操作与快捷命令。保留逻辑边界和组内位置，支持嵌套分组、撤销及搜索定位自动展开。
- 顶部格式工具栏统一为紧凑浮层，溢出时显示左右导航，窄窗格保留图标与数值；新增复制/粘贴外观入口。
- 改进右键拖动取消和菜单抑制、跨白板外观粘贴状态同步，避免折叠隐藏未完成的编辑；锁定内容约束也应用于分组对齐。

## 0.98.14

- 简化侧栏和白板工具的重复分隔，统一图标、菜单行高与键盘焦点；窄侧栏长标签自动截断。
- 普通白板的笔记卡片和文本支持 Enter 或 F2 就地编辑（思维导图保留 Enter 新建同级主题），保留输入法、锁定、暂停写入及连线操作保护。
- 点击底部缩放比例，可直接聚焦所选、适应全部或回到 100%；切换白板后旧菜单不会影响新白板。
- 格式工具栏刷新时保持横向滚动和控件焦点，避免修改样式后跳回最左侧。
- 子节点候选仅收集当前可见对象，批量转换减少临时集合与重复扫描；仍在一次事务中验证父子关系并支持撤销。

## 0.98.13

- 普通单向连接的父卡片旁新增“设为子节点并折叠”按钮；右键卡片也可直接操作，无需先选中细连线。
- 一次操作建立直接子节点关系并折叠，沿用加减号逐层展开；转换与折叠合并为一次撤销，保留原布局和连线样式。
- 转换前验证循环、多个父节点和锁定状态；不会隐藏正在编辑的子卡片，不自动转换双向或无箭头关联线。
- 按白板一次建立候选连线索引，避免逐卡片重复扫描连线。

## 0.98.12

- 收紧侧栏收集入口、快捷导航、搜索与白板列表的间距；统一对齐，减少顶部占位，选中行采用轻底色与细侧标。
- 统一白板侧边工具、更多菜单和顶部操作的尺寸；编辑格式栏更加紧凑，窄白板提前压缩底部控件文字，为标题和画布保留空间。
- 保持 Obsidian 字体、主题与透明卡片样式，保留现有卡片尺寸、拖拽、缩放和折叠逻辑。
- 发布后增加匿名版本查询和三个安装文件逐字节下载校验，检测“附件已上传但版本接口返回空列表”的安装异常。

## 0.98.11

- 为白板 JSON、保存设置、布局快照和可选 Obsidian 内部接口补充输入验证，降低无效数据导致运行时错误的风险。
- 清理编辑器、PDF、搜索与主模块的不安全类型访问和未使用代码；定时器优先绑定实际所属窗口，保留中文输入、撤销与源码编辑回退行为。
- 移除已经失效的重复样式声明，减少 !important 强制覆盖；保持现有明暗主题和透明卡片的计算样式。
- 新增英文说明、数据访问说明和贡献指南；发布改为 GitHub Actions 从标签构建并提供运行文件的构建来源证明。
- 发布附件使用 Obsidian 标准的 main.js、manifest.json、styles.css；本地仍可打包 ZIP。
- 包含 0.98.10 的卡片右下角缩放手柄与分支折叠按钮分离修复。

## 0.98.10

- 修复短卡片的分支折叠按钮遮住右下角手柄、导致拖动无法调整大小的问题；将折叠入口移到卡片右侧外部，与连线端口分开。
- 扩大调整大小手柄的命中范围，提高其层级，避免被卡片预览和附件控件遮挡。
- 白板缩小后的简略卡片也保留尺寸调整手柄，继续使用轻量预览。

## 0.98.9

- 子白板入口支持折叠为标题栏，通过卡片按钮、右键菜单和大纲恢复原大小；保存重载与撤销重做保留折叠状态。
- 折叠子白板时不加载缩略预览，保留进入白板的入口，兼容锁定对象和缺失文件。
- 验证父子分支折叠、逐层展开、全部展开及连线隐藏，补充回归覆盖。
- 修复折叠后残留编辑中子节点的边界问题：将要隐藏未完成的正文或标题编辑时暂停折叠，保留草稿和原布局。

## 0.98.8

- 空白处左键拖动画布、右键拖动框选；右键单击保留菜单。框选支持 Shift 累加，拖动完成后抑制同次菜单。
- 修复 SVG 预览和连线端点直接使用 aria-label 触发 Obsidian 原生提示框错误的问题，保留无障碍名称。

- 整理白板窗口统一参数面板、预览和底部操作的层级；窄矮窗口改为内容区滚动，保持应用与取消操作可见。
- 分组预览采用统一缩略图、两行标题和底部操作区，优化搜索、筛选、长标题与窄窗排列。
- 修复思维导图工作台最小高度冲突，底部操作保持在窗口内，工具页与预览使用剩余空间。
- 修复深色主题下导图模板预览的白底浅字问题；独立 SVG 导出继续保留原有配色。

## 0.98.7

- 侧栏快捷入口改为统一的轻量工具区，主导航用精细底线区分当前页面，白板列表增加短侧标和层级间距。
- 顶部格式栏减少重复色块，以字段分组、悬停和聚焦反馈表达层次，保持单行悬浮和横向滚动。
- 更多白板工具统一图标列和行距，分组标题在滚动时保持可见；底部画布背景操作增加分区与键盘聚焦提示。
- 原生白板标题栏简化入口底色，统一历史、状态与工作区操作的间距。

## 0.98.6

- 重新整理侧栏快捷入口、搜索与白板列表的视觉层级，统一对齐、选中状态和窄栏间距。
- 顶部格式栏采用紧凑字段分区，恢复被旧响应式样式隐藏的标签，继续保持单行悬浮与横向滚动。
- 修复创作工具栏分隔线不显示；选择、创作、组织和更多工具的分区更清楚。
- 底部画布控制在窄窗压缩文字，极窄窗采用明确的两行布局，避免与缩放控件挤占。

## 0.98.5

- 空白处按住左键拖动即可框选；Shift 累加选择，空格加左键或中键平移。取消框选恢复之前的选择。
- 多选后可在顶部切换“对象 / 连线”，统一修改对象样式，或批量修改连线路径、箭头、虚实线与颜色。
- 默认仅修改所选对象之间的连线，可显式切换“所有相连”；跳过锁定、隐藏或已删除的端点，防止旧工具栏误改重接后的连线。
- 批量目标连线高亮，每次修改作为一次撤销操作；框选期间不反复重建格式工具栏，沿用按帧合并的指针更新。

## 0.98.4

- 整理左侧快捷入口：空间总览、笔记摘录、整理白板和分组预览合并为两列工具区，减少重复边框，统一按钮与文字对齐。
- 优化搜索框、白板树行高和窄侧栏间距，保持原有导航、收藏、整理和分组入口。
- 减淡网格与点阵，画布底色跟随 Obsidian 主题；吸附状态保留颜色区分，并兼顾高对比度偏好。
- 顶部格式选择增加清晰的控件边界，统一悬浮工具栏圆角；保留单行滚动及原有编辑、媒体尺寸和连线逻辑。

## 0.98.3

- 修复 Obsidian 原生搜索结果打开索引 Markdown 而不是白板的问题；直接在搜索结果标签页打开实际白板。
- 点击白板内容命中项时定位对应对象；标题命中打开整张白板。保留普通 Markdown 笔记的原生打开行为。
- 对手动修改的索引、未保存编辑、已删除白板、快速切换和插件卸载增加保护，避免误跳转或覆盖编辑。

## 0.98.2

- 修复图片与 PDF 内容比外层卡片小、选中框和连线端点远离实际内容的问题。按媒体原始比例更新实际节点尺寸，不再仅隐藏边框。
- 删除图片插入时额外的 42 像素标题占位；PDF 按当前页尺寸适配，切换横竖页保留选定宽度。
- 图片、PDF 拖动缩放默认保持比例；异步测量沿用过期检查、锁定与折叠保护，避免覆盖用户正在进行的拖拽。

## 0.98.1

- 图片卡片移除文件名栏、底色和装饰边框，完整展示图片，不裁切内容。
- PDF 展开时只显示页面，翻页和折叠按钮在悬停或键盘聚焦时浮现；保留折叠状态下的文件标识及快捷键。
- 侧边“更多白板工具”增加“连线统一为直线”；选中连线后也可将当前路径应用到全部连线。
- 当前白板后续新连线、导图分支及导入连线沿用统一路径；支持撤销，并保留颜色、箭头、说明和关系结构。

## 0.98.0

- 增加 Obsidian 原生搜索索引：白板文字、标题、分组和连线说明支持搜索，并通过深链接定位节点。增量同步、内容校验与覆盖保护保留手动修改。
- PDF 卡片支持折叠、恢复尺寸和页码；选中卡片后可使用 PageUp / PageDown / Home / End 翻页。
- PDF 文档短期复用，限制闲置缓存和画布尺寸；取消渲染、文件更新、关闭视图和插件卸载时清理资源。
- 父子分支支持逐层展开和全部展开；普通连线可显式转换为父子分支，校验循环与多父节点。
- 新增搜索同步、PDF 生命周期、折叠和 5,000 层分支边界测试。

## 0.97.2

- 修复社区目录审核指出的静态样式设置与设置页标题问题，保留现有卡片、导图和写作布局。
- 将最低 Obsidian 版本改为已完成原生验收的 1.13.7，避免承诺未经验证的旧版兼容性。
- 修复文件列表定位时未等待延迟视图加载的问题。
- 补充英文功能与安装说明，并在 CI 中加入官方 Obsidian ESLint 检查。

## 0.97.1

- 将插件显示名称改为 `ThoughtSpace Whiteboard`，符合社区目录仅使用 Basic Latin 字符的命名规则。
- 插件 ID 保持 `thoughtspace`，沿用现有安装目录及设置。
- 此版本仅更新发布元数据和说明，白板运行代码与 0.97.0 一致。

## 0.97.0

### 连线

- 修复鼠标位于卡片内部却吸附到相邻卡片的问题；空隙中选择最近目标。
- 吸附范围按屏幕 20 px 计算，修正缩放与边角范围。
- 复用可见节点和连接期间的索引，减少重复画布测量与路径计算。
- 保留按帧合并输入、松手最终落点、重接、取消及撤销重做。

### 思维导图

- 拖线到空白处直接创建子主题、连接、排版并进入编辑。
- 增加方向键切换主题、Shift+Tab 返回父主题、F2 原位编辑。
- Tab / Enter 连续输入支持中文输入法组合输入保护。
- 新主题先测量再统一布局，避免重复整理。
- 优化草稿历史大小统计，保留 20 条 / 16 MB 上限。
- 修复相同文字的确认操作错误清除重做历史。
- 普通孤立对象保持方向键微调；连线等待状态不触发主题导航。

### 验证

1142 项自动测试通过，TypeScript 检查及构建通过。Obsidian 原生流程检查 34 项、真实鼠标/键盘检查 6 项通过。独立日历插件未包含在此发布包中。
